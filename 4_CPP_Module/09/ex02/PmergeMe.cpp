// 1. 무작위로 정렬된 배열이 있다.
// 2. 배열을 2개씩 짝을 지어 큰 애를 mainchain, 작은 애를 subchain으로 나눈다.
// 3. mainchain을 정렬한다. (정렬 방법은 똑같이 fj algo)
// 4. subchain을 mainchain에 하나씩 넣는다.
//     1. 2진 트리로 넣는다.
//     2. 넣는 순서는 jacobsthal 수열 순서로 넣는다.
