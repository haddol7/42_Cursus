const GAME_BOUNDS = { x: 5, y: 7 };
const PADDLE_WIDTH = 1;
const PADDLE_HEIGHT = 0.2;
const BALL_RADIUS = 0.2;
const WINNING_SCORE = 5;
const BALL_SIZE = { x: 0.4, y: 0.4, z: 0.4 };
const BALL_SPEED = 0.1;

module.exports = {
  GAME_BOUNDS,
  PADDLE_WIDTH,
  PADDLE_HEIGHT,
  BALL_RADIUS,
  WINNING_SCORE,
  BALL_SIZE,
  BALL_SPEED,
};
